require "spec_helper"

describe Hash, "#random" do

  property("empty") { fails? { Hash.new.random }}
  # .check

  property("member?"){|xs| guard(!xs.empty?); k, v = xs.random; xs.member?(k) }
  # .check { Hash.random { [Integer.random, String.random] }}
  # .check { Hash.random { [String.random, Integer.random] }}

end
